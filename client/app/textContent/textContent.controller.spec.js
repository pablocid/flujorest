'use strict';

describe('Controller: TextContentCtrl', function () {

/*  // load the controller's module
  beforeEach(module('flujorestApp'));

  var TextContentCtrl, scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller, $rootScope) {
    scope = $rootScope.$new();
    TextContentCtrl = $controller('TextContentCtrl', {
      $scope: scope
    });
  }));

  it('should ...', function () {
    expect(1).toEqual(1);
  });*/
});
