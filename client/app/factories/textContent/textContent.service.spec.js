'use strict';

describe('Service: textContent', function () {

/*  // load the service's module
  beforeEach(module('flujorestApp'));

  // instantiate service
  var textContent;
  beforeEach(inject(function (_textContent_) {
    textContent = _textContent_;
  }));

  it('should do something', function () {
    expect(!!textContent).toBe(true);
  });*/

});
